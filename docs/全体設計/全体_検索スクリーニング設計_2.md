了解。さっきの「1/3（全体方針）」を、**検索〜絞り込み〜管理〜執筆**の“ステップ順”で読みやすく書き直します（内容は同じで、構造だけ整理します）。
※まだ **2/3, 3/3** は出しません（あなたの指定通り、今回は1つ目だけ）。

---

# 1/3 全体方針：統合コーパスで「歴史」と「LLM」を扱う（手順ベース）

## ステップ0：基本方針（この文書で確定している前提）

* **統合コーパス（1コーパス）で進める**
  歴史・非ML古典・ML/LLM研究を、同じ母集団として集めて管理する。
* **LLMは網羅性重視**（可能な限り全件を拾う）
* **歴史は収集は同じ粒度で拾うが、記述は代表選定でまとめる**（全件を細かく解説しない）
* **統合コーパスのスクリーニングは「LLM必須」にしない**（非LLM古典も拾えるようにする）
* スクリーニング基準は **decompile_v8 をベースに改訂して使う**（改訂内容は未決・将来変更の可能性あり）

---

## ステップ1：検索（収集の入口）

### 1.1 方針（確定）

* **引用追跡（前方・後方）を必須**にする
  DB検索だけで落ちやすい古典・分野横断（PL/SE/SEC）を補う。
* **ミニQGSを必須**にする
  用語揺れ・DB偏りを補正するため、最初に少数の代表（seed/QGS）を持つ。

### 1.2 個別検索（確定・未実施）

* **NDSS と USENIX は個別に検索する予定**（まだ実施していないが方針として決定）

### 1.3 既存の検索ログ（参考として保持）

* これまでのDB検索・クエリ（あなたのJSON）は、**LLMパート単体を想定して練った試行**なので、

  * 統合コーパス方針の「正式ベースライン」としては固定しない（=決定事項ではない）
  * ただし今後の再設計の参照にするため、**DB・クエリ・検索日・件数を文書に残す**（参照用）

### 1.4 未決（ここで今後検討すること）

* 使うDBの最終セット（追加/削除）
* 検索語彙の最終形（`decompil*`以外の語彙拡張）
* 検索の停止条件、QGS規模、ログ粒度

---

## ステップ2：統合（重複排除・コーパス化）

### 2.1 方針（確定）

* 複数ソースから集めた結果を **1つの統合コーパスに集約**して管理する。
* 同一論文が「歴史の節目」かつ「LLM対象」になり得るため、統合後は **複数ラベル（タグ）で管理する**（二重カウントではなく“同一レコードの複数タグ”）。

### 2.2 未決

* 重複排除の具体ルール（DOI優先、タイトル正規化など）
* タグ体系（歴史タグ／LLMタグ／コンポーネントタグの関係）

---

## ステップ3：スクリーニング（含む/除外の判断）

### 3.1 方針（確定）

* 統合コーパスのスクリーニングは **「LLM必須」にしない**
  → 非LLMの古典も拾えることを優先。
* スクリーニング基準は **decompile_v8 をベースに改訂して運用する**
  → “ベースにする”は確定、改訂の中身は未決（後で変える可能性も許容）。

### 3.2 未決（ただし後で必ず決める）

* v8をどう一般化するか（例外枠の設け方／上位抽象化）
* 統合スコープの境界（disassembly/liftingのみ等をどう扱うか）

---

## ステップ4：まとめ方（歴史とLLMでの扱い分け）

### 4.1 LLMパートの扱い（確定）

* **LLM関連は原則すべて扱う**（網羅性重視で分類・整理する）

### 4.2 歴史パートの扱い（確定）

* 収集段階では統合コーパスの一部として **同じ粒度で拾う**
* 記述段階では **代表（節目）を選んで説明**する（全件を個別解説しない）

### 4.3 未決

* 「代表」の決め方（選定基準・採択数）
* 歴史の分類軸（例：8コンポーネント）をどこまで本文で使うか

---

## ステップ5：検討事項として保持（いまは決めない）

* **史実台帳（Tier運用）を採用するか**
  （ツール史・公開年など“史実”の根拠管理方法。現時点では検討事項。）

---

次は **2/3：歴史パートの詳細方針（同じくステップ順）** を出します。
