(
  (
    TI=("source recovery") OR AB=("source recovery") OR
    TI=("source code recovery") OR AB=("source code recovery") OR
    TI=("code recovery") OR AB=("code recovery") OR
    TI=("source reconstruction") OR AB=("source reconstruction") OR
    TI=("binary-to-source") OR AB=("binary-to-source") OR
    TI=("binary to source") OR AB=("binary to source") OR
    TI=("assembly-to-C") OR AB=("assembly-to-C") OR
    TI=("binary-to-C") OR AB=("binary-to-C") OR
    TI=("lifting") OR AB=("lifting")
  )
  AND
  (
    TI=(binary) OR AB=(binary) OR
    TI=(assembly) OR AB=(assembly) OR
    TI=(executable) OR AB=(executable) OR
    TI=("machine code") OR AB=("machine code") OR
    TI=(disassembl*) OR AB=(disassembl*) OR
    TI=(bytecode) OR AB=(bytecode)
  )
  AND
  (
    TI=("machine learning") OR AB=("machine learning") OR
    TI=("deep learning") OR AB=("deep learning") OR
    TI=(neural network*) OR AB=(neural network*) OR
    TI=(transformer*) OR AB=(transformer*) OR
    TI=(language model*) OR AB=(language model*) OR
    TI=(LLM) OR AB=(LLM) OR
    TI=(GPT) OR AB=(GPT)
  )
)
